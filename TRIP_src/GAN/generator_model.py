import torch
import torch.nn as nn

# Initialize generator
class Generator(nn.Module):
    def __init__(self):
        super().__init__()
        self.gen = nn.Sequential(

        )

    def forward(self, x):
        return self.gen(x)